<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1, user-scalable=no, minimum-scale=1, height=device-height, target-densitydpi=device-dpi"/>
<%= include_gon %>
   
  <title>PairBnB</title>
    

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

    <link href='https://fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css'>
  
   

</head>
<body>
  <div id="header">
    <% if signed_in? -%>
      <%= button_to ".sign_out", sign_out_path, method: :delete %>
    <% else -%>
      <%= link_to ".sign_in", sign_in_path %>
    <% end -%>
  </div>

  <div id="flash">
    <% flash.each do |key, value| -%>
      <div id="flash_<%= key %>"><%=h value %></div>
    <% end %>
  </div>

  <%= render partial: "layouts/navbar" %>
    <div class="container">
      <%= yield %>
    </div>
    <div class="pusher"></div>
    <div id="footer">
      <ul>
        <li>About Us</li>
        <li>Privacy</li>
        <li>Terms</li>
        <li>Contact Us</li>
        <li>Signup for newsletter</li>
      </ul>
    </div>
  </body>
</html>
